# Optional but strongly recommended for Cesium World Terrain / imagery.
# Create a token at https://ion.cesium.com/
VITE_CESIUM_ION_TOKEN=
# Backend-side origin and optional allowed origin.
PORT=10000
CORS_ORIGIN=*
